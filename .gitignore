# Environments and caches
.venv/
__pycache__/
*.pyc
.pytest_cache/
*.egg-info/
build/
dist/

# Analysis runs (working folders created by the tool)
runs/

# FEM models and results are never versioned
*.fez

# Office lock files
~$*.xlsx
~$*.docx

# OS noise
Thumbs.db
.DS_Store

# Editor settings (local to each machine)
.vscode/

# Local project-memory / working notes (kept out of the public repo)
CLAUDE.md
ESTADO.md

# Native installer packaging: the .exe is distributed by the author, not built
# from the public repo (the bundle build scripts in tools/ remain public).
installer/
